export enum ClientRoutes {
  CONNECT = "SC_CONNECT",
  DISCONNECT = "SC_DISCONNECT",
  PONG = "SC_PONG",
}

export enum ServerRoutes {
  CONNECT = "CS_CONNECT",
  DISCONNECT = "CS_DISCONNECT",
  PING = "CS_PING",
}
